# Konfiguration

Hier liegen ausschließlich **öffentliche Beispiele oder dokumentierte Standardwerte**.

Private/lokale Konfiguration gehört nach `config/local/` und wird von Git ignoriert.

Keine Tokens, Accounts, E-Mails, Pfade mit privaten Daten oder Passwörter committen.
